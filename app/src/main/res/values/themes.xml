<resources>
    <!-- Base AppCompat theme -->
    <style name="Base.Theme.NEPA" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/nepa_black</item>
        <item name="colorPrimaryDark">@color/nepa_black</item>
        <item name="colorAccent">@color/nepa_white</item>

        <item name="android:statusBarColor">@color/nepa_black</item>
        <item name="android:navigationBarColor">@color/nepa_black</item>
    </style>

    <!-- App theme -->
    <style name="Theme.NEPA" parent="Base.Theme.NEPA" />
</resources>
